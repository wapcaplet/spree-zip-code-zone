<%= render :partial => 'admin/shared/configuration_menu' %>

<h1><%= t('editing_zip_code_range') %></h1>

<%= error_messages_for :zip_code_range %>

<% form_for(:zip_code_range, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= render :partial => 'admin/shared/edit_resource_links' %>
<% end %>

